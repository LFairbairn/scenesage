services:

  streamlit:
    build:
      context: .
      dockerfile: app/Dockerfile
    ports:
      - "8501:8501"
    environment:
      - OLLAMA_URL=http://host.docker.internal:11434
    volumes:
      - chromadb-data:/app/data/chromadb
    
volumes:
  chromadb-data:
